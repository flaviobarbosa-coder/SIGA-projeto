<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #8B0000;">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="/dashboard">🏛️ SIGA</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <% if (locals.utilizador) { %>
          <% if (['admin'].includes(locals.utilizador.perfil)) { %>
            <li class="nav-item">
              <a class="nav-link" href="/utilizadores">👥 Utilizadores</a>
            </li>
          <% } %>
          <% if (['admin','gestor_associados'].includes(locals.utilizador.perfil)) { %>
            <li class="nav-item">
              <a class="nav-link" href="/associados">🤝 Associados</a>
            </li>
          <% } %>
          <% if (['admin','gestor_financeiro'].includes(locals.utilizador.perfil)) { %>
            <li class="nav-item">
              <a class="nav-link" href="/quotas">💰 Quotas</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/financeiro">🧾 Financeiro</a>
            </li>
          <% } %>
          <% if (['admin','gestor_atividades'].includes(locals.utilizador.perfil)) { %>
            <li class="nav-item">
              <a class="nav-link" href="/atividades">📅 Atividades</a>
            </li>
          <% } %>
          <% if (['admin','direcao'].includes(locals.utilizador.perfil)) { %>
            <li class="nav-item">
              <a class="nav-link" href="/dashboard">📊 Dashboard</a>
            </li>
          <% } %>
        <% } %>
      </ul>
      <ul class="navbar-nav">
        <% if (locals.utilizador) { %>
          <li class="nav-item">
            <span class="nav-link text-white-50">👤 <%= locals.utilizador.nome %></span>
          </li>
          <li class="nav-item">
            <a class="nav-link text-warning" href="/auth/logout">🚪 Sair</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
