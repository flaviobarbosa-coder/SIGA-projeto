<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #8B0000;">
  <div class="container">
    <a class="navbar-brand" href="/">🏛️ SIGA</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">

        <% if (session && session.utilizador) { %>

          <% if (session.utilizador.perfil === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link" href="/utilizadores">Utilizadores</a>
            </li>
          <% } %>

          <% if (session.utilizador.perfil === 'gestor_associados') { %>
            <li class="nav-item">
              <a class="nav-link" href="/associados">Associados</a>
            </li>
          <% } %>

          <% if (session.utilizador.perfil === 'gestor_financeiro') { %>
            <li class="nav-item">
              <a class="nav-link" href="/financeiro">Financeiro</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/quotas">Quotas</a>
            </li>
          <% } %>

          <% if (session.utilizador.perfil === 'gestor_atividades') { %>
            <li class="nav-item">
              <a class="nav-link" href="/atividades">Atividades</a>
            </li>
          <% } %>

          <% if (session.utilizador.perfil === 'direcao') { %>
            <li class="nav-item">
              <a class="nav-link" href="/dashboard">Dashboard</a>
            </li>
          <% } %>

          <% if (session.utilizador.perfil === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link" href="/associados">Associados</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/financeiro">Financeiro</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/quotas">Quotas</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/atividades">Atividades</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/dashboard">Dashboard</a>
            </li>
          <% } %>

        <% } %>

      </ul>
      <ul class="navbar-nav">
        <% if (session && session.utilizador) { %>
          <li class="nav-item">
            <span class="nav-link text-white-50">
              👤 <%= session.utilizador.nome %>
            </span>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/auth/logout">Sair</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
